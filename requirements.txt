# For Streamlit Community Cloud, which installs from requirements.txt rather
# than pyproject.toml. Keep in sync with the dependencies in pyproject.toml.
requests>=2.31
python-dotenv>=1.0
pandas>=2.2
streamlit>=1.58.0
plotly>=6.8.0
